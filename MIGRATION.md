# Migration From Upstream

Migration guidance has moved to `docs/manual.md#8-从上游迁移`.

That section now covers:

- the one-line migration command and version-specific example
- what `--migrate` does step by step
- preserved data and automatic rollback behavior
- post-migration `--update` and `--force-update` behavior
